"""
Autonomous agents that use ``src/llm`` and ``src/github_issues_mcp``.

Each subpackage is one agent with its own runner, tools, and validator.
MCP tools for Cursor live separately under ``github_issues_mcp/``.
"""
